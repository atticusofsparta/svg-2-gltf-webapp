{
  "name": "svg-to-gltf-webapp",
  "version": "1.0.0",
  "type": "module",
  "description": "Web-based SVG to GLTF converter using Three.js",
  "scripts": {
    "dev": "vite",
    "prebuild": "node scripts/bundle-source.mjs",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "archiver": "^7.0.1",
    "vite": "^6.0.0"
  }
}
